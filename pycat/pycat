#!/usr/bin/env python

import argparse


parser = argparse.ArgumentParser(description='print some files to stdout')
parser.add_argument('file', type=str, nargs='?', help='files to cat')

args = parser.parse_args()

with open(args.file, "r") as f:
    print(f.read())

